apiVersion: v1
kind: Service
metadata:
  name: churn-analysis
  labels:
    app: churn-analysis
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: churn-analysis 